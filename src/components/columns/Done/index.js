import React from 'react';

import BaseColumn from '../BaseColumn';

const Done = () => (
    <BaseColumn heading="Done" className="column__done">
        <p>content</p>
    </BaseColumn>
);

export default Done;
