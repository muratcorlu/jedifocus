import React from 'react';

import BaseColumn from '../BaseColumn';

const Done = () => (
    <BaseColumn
        bucket="done"
        heading="Done"
        className="column__done"
        items={{}}
    />
);

export default Done;
