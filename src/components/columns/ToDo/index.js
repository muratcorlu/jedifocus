import React from 'react';

import BaseColumn from '../BaseColumn';

const Todo = () => (
    <BaseColumn heading="To Do" className="column__to-do">
        <p>content</p>
    </BaseColumn>
);

export default Todo;
